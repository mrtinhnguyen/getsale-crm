{
  "name": "getsale-crm",
  "version": "1.0.0",
  "description": "AI CRM SaaS - Enterprise Microservices Platform",
  "private": true,
  "workspaces": [
    "services/*",
    "shared/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "docker-compose up -d",
    "dev:logs": "docker-compose logs -f",
    "dev:down": "docker-compose down",
    "dev:clean": "docker-compose down -v",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "typecheck": "npm run typecheck --workspaces",
    "migrate": "cd migrations && npm run migrate",
    "migrate:rollback": "cd migrations && npm run migrate:rollback",
    "migrate:status": "cd migrations && npm run migrate:status",
    "migrate:make": "cd migrations && npm run migrate:make",
    "seed": "cd migrations && npm run seed",
    "stage2-closure": "node scripts/stage2-closure-check.mjs",
    "stage3-ids": "node scripts/print-stage3-ids.mjs",
    "stage3-e2e": "node scripts/stage3-e2e-test.mjs"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/supertest": "^6.0.3",
    "@vitest/coverage-v8": "^3.2.4",
    "pg": "^8.11.3",
    "supertest": "^7.2.2",
    "typescript": "^5.3.0",
    "vitest": "^3.2.4"
  }
}
