/**
 * @deprecated SSE real-time channel removed — consolidated to WebSocket (Socket.IO).
 * Events previously routed here now flow through websocket-service via Redis bridge.
 * This file is kept empty to preserve git history.
 */
